#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * @n: The integer number
 *
 * Description: This function computes the absolute value of the input number.
 *
 * Return: The absolute value of the integer
 */
int _abs(int n)
{
if (n < 0)
return (-n);
else
return (n);
}

